# アップロード手順（2026-08-31）

このフォルダの **1ファイル** をGitHubに上げます。

| 送るもの | 大きさ | |
|---|---|---|
| `admin.html` | 46.4 KB | 差し替え（スコア入力画面） |

**ほかは全部そろっています。** index.html・u8.html・u9.html・parking.html と画像44枚は、
公開中のものと中身が一致していることを確認ずみです。

---

## 手順

1. ブラウザで **https://github.com/gambaboy0/natsuyasumi-cup** を開く
2. **`Add file` → `Upload files`** を押す
3. このフォルダの **`admin.html` だけ**を、画面にドラッグして落とす
   - ※ この手順書（`アップロード手順.md`）は**上げないこと**
4. 下の `Commit changes` を押す

## 上げたあとに必ずすること

上げ終わったら舟見さんから声をかけてください。こちらで
**公開URLからファイルを取り直して、手元とハッシュが一致するか照合**します。

---

## 今回の変更内容（スコア入力画面）

### ① リンクを開いたらすぐ入力画面

合言葉を入れる画面をとばせるようにしました。

- URLの末尾に **`?k=合言葉`** を付けたリンクを開くと、そのまま入力画面になります
- 合言葉は**HTMLの中に書いていません**。`admin.html` だけを開いた人には、
  これまでどおり合言葉をたずねます
- 一度開くと端末が覚えるので、2回目からは `?k=` なしでも開けます

> ⚠️ **このリンクは当日の担当者だけに送ること。**
> リンクそのものに合言葉が入っているので、転送されると誰でも入力できます。

### ② ④確認する を追加

いま選んでいるカテゴリの全試合を、**HPの日程とまったく同じ構成**（縦＝時刻／横＝コート）で表示。
マスの中身も日程と同じ（Match No.→段階・組名→紋章→チーム名→スコア→チーム名→紋章）。

- **まだ入力していない試合のマスは灰色**。入力すると白くなるので、残りがひと目で分かる
- スコアを送ると、この表もその場で新しくなる
- 紋章を出すため、28チーム分の対応表を u8.html / u9.html から写して持たせている
  ★チーム名や紋章の画像を変えたときは、向こうと合わせてここも直すこと

### ③ 同着の順位に「リーグ戦の順位表」を追加

「⚠️ 同着の順位」を押すと、注意書きの下に**そのカテゴリの順位表を全部**表示します。

- U8は12枚（予選A〜F＋1〜6組）、U9は4枚（グループA〜D）
- **同じ順位が2つ以上ある行に色が付く**ので、どこで抽選が要るか一覧で探せる
- 「🔄 最新にする」でいつでも読み直せる

**スプレッドシートには一切さわっていません。**すべて画面の表示だけの変更です。

## 確認ずみのこと

- `<script>` の構文チェック（node --check）… OK
- **U8の全51試合・U9の全40試合を実際に入れ切るテストを実施**（2026-08-30）
  - 予選 → クラスリーグ → トーナメント → 最終順位 まで自動でつながることを確認
  - **PK戦**… 入れるまで次の相手が決まらず、入れると勝者が正しく流れる。入れ直すと入れ替わる
  - ④確認する … 51マス・GL/CL/KRの3表・灰色の増減が正しい
  - 同着の順位 … 順位表12枚を表示
  - **テスト後、スコアはすべて消して0件に戻してある**
- 順位の手入力（rank_override）は**テストしていない**（舟見さん指示で中止）。
  順位表の数式は無傷。
