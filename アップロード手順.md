# アップロード手順（2026-08-30 3回目）

このフォルダの **2ファイル** をGitHubに上げます。

| 送るもの | 大きさ | |
|---|---|---|
| `u8.html` | 160.1 KB | 差し替え |
| `u9.html` | 156.4 KB | 差し替え |

## 2回目のぶんは反映ずみです

**`index.html`・`logo_parking.png`・`logo_water.png` は、公開中のものと中身が一致していました。**
（HTML5本と画像44枚を1つずつ取得して照合ずみ）
2回目の送付フォルダは `_過去の送付\_アップロード_2026-08-30_2回目\` へ移しました。

---

## 手順

1. ブラウザで **https://github.com/gambaboy0/natsuyasumi-cup** を開く
2. **`Add file` → `Upload files`** を押す
3. このフォルダの **`u8.html` と `u9.html` の2つだけ** を、画面にドラッグして落とす
   - ※ この手順書（`アップロード手順.md`）は**上げないこと**
4. 下の `Commit changes` を押す

**同じ名前のファイルは自動で新しいほうに置きかわります。**

## 上げたあとに必ずすること

上げ終わったら舟見さんから声をかけてください。こちらで
**公開URLからファイルを取り直して、手元とハッシュが一致するか照合**します。
それが済むまでは「完了」としません。

## 見るときの注意

GitHub Pages は10分間ページを覚えています。
すぐ確認したいときは **URLの末尾に `?1` を付ける**と確実です。

- https://gambaboy0.github.io/natsuyasumi-cup/u8.html?1
- https://gambaboy0.github.io/natsuyasumi-cup/u9.html?1

---

## 今回の変更内容 ─ チーム専用ページ

参加チーム一覧のマスをタップすると開く、チームごとのページを直しました。

1. **チーム正式名称の黒帯を削除**
   - 背景をなくして**黒文字**に。下に**黒いアンダーライン**（文字の幅にぴったり）
   - もともと `<h2>` だったため、項目の見出し（黒帯）の見た目を受け継いでいた
2. **日程を、順位決定戦と同じ「横長・2チーム横並びのカード」に変更**
   - エンブレム／チーム名／Match No.・コート・時刻／チーム名／エンブレム
   - **順位決定戦との違いは、黒帯の見出しを置かないこと**
   - KNOCKOUT ROUND と同じ部品（`koRow`）を呼んでいるので、**直すときは1か所でよい**
3. **日程の枠を、対戦表と同じ灰色の線で囲んだ**（4辺すべて／試合の間にも線）
4. **日程と対戦表のあいだにあった「成績の8マス」を削除**
   - 勝点・勝・分・負・得点・失点・得失差・順位。同じ数字が対戦表の右側にもあり二重だった
   - `statsHtml()` は残してあるので、戻したくなったら呼び直すだけ
5. **U8はクラスリーグの順位表も空欄で表示**
   - 「クラスリーグ（組はグループリーグの結果で決まります）」として3チーム分の空欄の表
   - 何チームで競るのかが先に分かるようにするため
   - U9はクラスリーグが無いので入れていない

**スプレッドシートには一切さわっていません。**すべてHPの表示だけの変更です。

## 確認ずみのこと

- `<script>` の構文チェック（node --check）… u8・u9とも OK
- `statsHtml` の呼び出しが0件になっていることを確認
- 本番スプレッドシートを読ませて、チーム専用ページを実際に開いて目視

## 送らないもの

手元の `assets\` には、どのページからも使われていない古い画像が5枚あります
（`logo_title.png` `mainmenu.jpg` `mainmenu_top.png` `mainmenu_bottom.png` `opening_emblem.jpg`）。
**公開側にも無く、使ってもいないので送りません。** 大会後の整理でまとめて片付けます。
